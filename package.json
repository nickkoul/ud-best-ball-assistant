{
  "name": "ud-best-ball-draft-helper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bun run build.js",
    "dev": "bun run build.js -- --watch",
    "test": "bun test --watch"
  },
  "devDependencies": {}
}
